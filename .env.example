# API URL para desarrollo local (frontend)
VITE_API_URL=http://localhost:3001/api

# --- Variables del backend (server/.env o dashboard de Vercel) ---
# Cadena de conexion de Neon PostgreSQL
DATABASE_URL=postgresql://usuario:password@host/neondb?sslmode=require

# Credenciales de Gmail para envio de correos (opcional)
# Si no se configuran, los tokens de verificacion se imprimen en consola
EMAIL_USER=tu-email@gmail.com
EMAIL_PASS=tu-app-password-de-gmail

# Para produccion en Vercel, configura VITE_API_URL en el dashboard del frontend:
# VITE_API_URL=https://tu-backend.vercel.app/api